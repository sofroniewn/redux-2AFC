var dx = require('hxdx').dx
var actions = require('../actions/choice.js')

module.exports = function (state) {
  // function zero () {
  //   dx(actions.zero())
  // }

  // function one () {
  //   dx(actions.one())
  // }

  return {
    'DI0': 1,
    'DI1': 2,
  }
}